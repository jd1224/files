Script will take a list of input ip addresses in .csv format named input.csv and output the virustotal information to a folder in the
same directory with named as the IP address.  requires python requests module which can be downloaded with pip install requests
*************************designed and run on windows 10 with python 3*******************************************************
reuires free api key from virustotal.com
